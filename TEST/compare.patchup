SetInputFolder
    ../
SetOutputFolder
    /tmp/

ReadInputFiles
    pendown.d
    pendown.js

ReplaceTabulations
RemoveLastSpaces

FindFiles
    pendown.d

ReplaceUnquotedText?
    ` ==`
    ` ===`

ReplaceUnquotedText?
    ` !=`
    ` !==`

ReplaceUnquotedText?
    ` ~ `
    ` + `

ReplaceUnquotedText?
    ` ~=`
    ` +=`

ReplaceUnquotedText?
    ` !is null`
    ` !== null`

ReplaceUnquotedText?
    ` is null`
    ` === null`

ReplaceUnquotedText?
    `.to!dstring()`
    ``

ReplaceUnquotedText?
    `.to!string()`
    ``

ReplaceUnquotedText?
    `.to!long()`
    ``

ReplaceUnquotedText?
    `.to!double()`
    ``

ReplaceUnquotedText?
    `.to!double()`
    ``

ReplaceExpression?
    `replace\\( ("[^"]+"), ("[^"]*") \\)`
    `split( $1 ).join( $2 )`

FindFiles
    pendown.js

ReplaceUnquotedText?
    `.charCodeAt( 0 )`
    ``

WriteOutputFiles
