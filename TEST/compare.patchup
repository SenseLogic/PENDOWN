SetInputFolder
    ../
SetOutputFolder
    /tmp/

--

ReadFiles
    pendown.d
    pendown.js
ReplaceTabulations
RemoveLastSpaces
WriteFiles

--

SetFolder
    /tmp/

--

ReadFiles
    pendown.d
ReplaceUnquotedText
    ` ==`
    ` ===`
ReplaceUnquotedText
    ` !=`
    ` !==`
ReplaceUnquotedText
    ` ~ `
    ` + `
ReplaceUnquotedText
    ` ~=`
    ` +=`
ReplaceUnquotedText
    ` !is null`
    ` !== null`
ReplaceUnquotedText
    ` is null`
    ` === null`
ReplaceUnquotedText
    `.to!dstring()`
    ``
ReplaceUnquotedText
    `.to!string()`
    ``
ReplaceUnquotedText
    `.to!long()`
    ``
ReplaceUnquotedText
    `.to!double()`
    ``
ReplaceUnquotedText
    `.to!double()`
    ``
ReplaceExpression
    `replace\\( (.+?), (.+?) \\)`
    `split( $1 ).join( $2 )`
WriteFiles

--

ReadFiles
    pendown.js
ReplaceUnquotedText
    `.charCodeAt( 0 )`
    ``
WriteFiles
